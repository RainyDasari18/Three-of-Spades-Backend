services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: spades
      POSTGRES_PASSWORD: spades
      POSTGRES_DB: three_of_spades
    ports:
      - "5432:5432"
    volumes:
      - spades_pg:/var/lib/postgresql/data

volumes:
  spades_pg:
